# VITHELO 首页第 08 屏询盘版式设计规范

日期：2026-09-03  
状态：用户已确认方案 B  
范围：首页 `08 · Start a Project` 单屏

## 1. 目标

将首页第 08 屏重排为编辑式询盘收口：左侧建立标题层级，右侧提供 Email 与 WhatsApp 两个直接联系入口，下方保留项目填写区。此次只改变排版与必要的交互承载方式，不改变现有品牌颜色，不修改前七屏。

## 2. 已确认版式

采用方案 B「编辑式左右分栏」。

- 上部左栏：`08 · Start a Project`、主标题和简短说明。
- 上部右栏：Email 与 WhatsApp 两张等宽入口卡片。
- 下部：横跨内容宽度的项目填写区。
- 最下方：保留现有 `Made for what comes next.` 品牌收尾。
- 桌面端使用非对称左右分栏；移动端按标题、Email、WhatsApp、填写区顺序变为单列。

## 3. 联系入口

- Email 卡片整张可点击，使用当前配置邮箱 `wang26098@gmail.com`，通过 `mailto:` 打开用户默认邮件应用。
- WhatsApp 卡片整张可点击，使用当前配置号码 `+86 182 7366 9556`，在新标签页打开 WhatsApp 对话。
- 卡片需要明确显示渠道名称、当前联系方式、动作说明和方向箭头。
- 不增加 Brief Form 第三个入口，不展示虚构销售团队或账户经理信息。

## 4. 项目填写区

保留以下五项：

1. Your brand or company
2. Dosage form
3. Estimated volume
4. Primary market
5. Project summary

填写区底部保留两个动作：

- Continue by Email：把当前填写内容编码进 Email 主题与正文后打开邮件应用。
- Continue on WhatsApp：把当前填写内容编码进 WhatsApp 消息后打开对话。

页面不提交或保存数据，继续显示隐私说明 `Your information is not stored on this website.`。空字段沿用现有 `Not provided` 逻辑，不增加验证门槛。

## 5. 视觉约束

- 只使用现有 Ivory、Graphite、Titanium 与现有透明度/边线变量。
- 不引入参考图的绿色、白色圆角卡片主题或新阴影系统。
- 卡片与填写区通过现有色块、细边线、留白和字号层级建立关系。
- 不改变全站字体体系、导航、Hero 或其他页面。
- 入口卡片可使用克制的位移或箭头响应；不得加入无意义漂浮、弹跳或持续动画。

## 6. 可访问性与响应式

- 两张入口卡片使用语义化链接，保留可识别的 `aria-label`。
- 键盘焦点必须清晰，点击目标不小于 44px。
- WhatsApp 外链保留 `rel="noreferrer"`。
- 760px 以下改为单列，字段也改为单列，不产生横向滚动。
- Reduced Motion 下所有内容与功能保持完整可见。

## 7. 实现边界

预计只修改第 08 屏组合、询盘填写组件的动作标签，以及该屏相关 CSS。联系方式继续从 `siteConfig` 读取，消息链接继续使用 `src/lib/inquiry.ts`，不把联系方式或表单内容硬编码到布局组件。

不实施：站内提交、后端接口、CRM、数据存储、分析追踪、隐私同意流程、方案 3 表单服务。

## 8. 验收标准

- 第 08 屏在桌面端呈现确认的 B 方案层级。
- Email 与 WhatsApp 顶部卡片可以直接打开对应应用。
- 填写后的两个底部按钮包含当前字段内容。
- 现有颜色不变，前七屏不变，品牌收尾保留。
- 桌面和移动端无重叠、裁切或横向溢出。
- 相关单元测试、询盘 E2E、lint、typecheck 和 build 通过。
